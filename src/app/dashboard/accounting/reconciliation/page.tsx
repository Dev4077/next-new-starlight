import Reconciliation from '@/features/accounting/reconciliation/Reconciliation';

export const metadata = {
  title: 'Dashboard : Reconciliation view'
};

export default function page() {
  return <Reconciliation />;
}
