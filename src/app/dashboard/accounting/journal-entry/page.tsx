import JournalEntry from '@/features/accounting/journalEntry/JournalEntry';

export const metadata = {
  title: 'Dashboard : Journal Entry view'
};

export default function page() {
  return <JournalEntry />;
}
