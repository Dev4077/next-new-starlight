import CreditCards from '@/features/banking/creditCards/CreditCards';

export const metadata = {
  title: 'Dashboard : Credit Cards view'
};

export default function page() {
  return <CreditCards />;
}
