import BankAccount from '@/features/banking/bank-account/BankAccount';

export const metadata = {
  title: 'Dashboard : Bank Account view'
};

export default function page() {
  return <BankAccount />;
}
