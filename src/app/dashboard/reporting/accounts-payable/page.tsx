import AccountsPayable from '@/features/reporting/accountsPayable/AccountsPayable';

export const metadata = {
  title: 'Dashboard : Accounts Payable view'
};

export default function page() {
  return <AccountsPayable />;
}
