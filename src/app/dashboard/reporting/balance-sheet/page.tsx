import BalanceSheet from '@/features/reporting/balanceSheet/BalanceSheet';

export const metadata = {
  title: 'Dashboard : Balance Sheet view'
};

export default function page() {
  return <BalanceSheet />;
}
